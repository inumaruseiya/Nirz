# 1) Copy this file to `.env` (gitignored) and fill in real values.
# 2) Run from repo root with either:
#    - VS Code / Cursor: デバッグ構成「Nirz」（.vscode/launch.json が --dart-define-from-file=.env を付与）
#    - CLI: flutter run --dart-define-from-file=.env
# または従来どおり:
# flutter run --dart-define=SUPABASE_URL=... --dart-define=SUPABASE_ANON_KEY=...
#
# Do not commit real keys. Values below are placeholders only.

SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key
